============
Installation
============

At the command line::

    $ pip install chenjie_novaclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv chenjie_novaclient
    $ pip install chenjie_novaclient
