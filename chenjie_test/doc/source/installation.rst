============
Installation
============

At the command line::

    $ pip install chenjie_nova

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv chenjie_nova
    $ pip install chenjie_nova
